# Excel lock files
~$*.xlsx

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Personal test workbooks (contain real financial data — never commit)
*_Test*.xlsx
*TestRun*.xlsx
*Test Run*.xlsx

# Private / stealth planning & development — do not publish
private/

# Session scratch artifacts
outputs/

# Monthly Budget Tracker — private data & personal config (never publish)
Personal Expense*.xlsx
Monthly Budget Tracker Dennis*.xlsx
Migrated*.xlsx
Reconciliation*.xlsx
*categorised draft*.xlsx
rules.csv
migrate_to_entries.py

# Statement importer — descoped from the initial release; needs testing against
# real bank/card statements before publishing. Re-enable in a follow-up PR.
categorize_expenses.py
rules.example.csv

# Bank / brokerage / card statement exports (real account data — never publish)
UOB*.xlsx
POSB*.xlsx
posb*.xlsx
*Account From*.xlsx
*Statement*.pdf
*Statement*.xlsx

# Statement importer working files (scaffolded from real financial data)
mapping.xlsx
mapping-before-*.xlsx
Entries draft*.xlsx

# Scratch / working dirs (previews & intermediates derived from real data)
tmp/
.tmp/

# Statement importer helper (macOS PDF backend) — descoped with the importer
tools/
